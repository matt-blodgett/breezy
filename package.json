{
  "name": "breezy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "web-ext run --source-dir=./src/",
    "prettier": "prettier --write ./src/"
  },
  "devDependencies": {
    "prettier": "3.1.0",
    "webextension-polyfill": "^0.10.0"
  }
}
